# 让 AI 优化代码性能，怎么保证它不把逻辑改错？

我做过不少 AI 辅助代码优化的尝试，结论是：**当前 AI 在"改快"这件事上已经够强了，真正的难题是"怎么证明改完之后又快又对"。**

## AI 优化代码这件事，坑在哪？

我用过几个主流 AI 编程工具做性能优化，反复遇到三个问题。不是某个工具的问题，是这类工具的系统性短板。

### 第一，AI 改完代码没法证明正确性

模型改了循环结构、换了数据类型、调整了返回顺序，它告诉你"逻辑没变"。但你怎么验证？普通单元测试覆盖不到所有边界条件、错误路径和状态转换。

比如一个排序函数，AI 把快换成了堆排序，输出确实是有序的，但**稳定性变了**——原来相等元素的相对顺序保持不变，现在变了。这种改变不会触发任何测试失败，但依赖稳定排序的上游逻辑可能就坏了。

### 第二，性能数据不可信

AI 说"快了 30%"，你问它怎么测的——单次 `time` 命令。但单次测量受系统负载、CPU 缓存状态、解释器启动时间影响极大。同一段代码跑十次，结果可能差 20%。这种波动下，"快了 30%"这个数字几乎没有统计意义。

真正可信的性能比较需要：warmup 预热、多次重复采样、基线和候选交替执行（减少机器状态漂移）、中位数而非平均值、变异系数检查、置信区间。这些在通用 AI 工具里几乎没有。

### 第三，AI 倾向于局部修补，不做算法级分析

你让 AI 优化一段代码，它大概率做的是：改循环边界、换内置函数、加个缓存。这些改法有效但有限。

真正的性能突破往往在算法层面——**O(n²) 到 O(n log n) 的跨越不是改循环能做到的**，需要重新分析问题结构、输入分布和复杂度差距。但大多数 AI 工具不会主动做这种分析，因为算法级重构比局部修补风险高得多，模型会倾向于选择"安全但收益有限"的改法。

## 我找到的一个解法

最近在 GitHub 上看到一个开源项目 [Algocode](https://github.com/acfoundry/Algocode)，它的思路跟主流 AI 编程工具很不一样。核心设计就是针对上面三个问题做的。

### 怎么解决"不证正确性"——三层验证体系

Algocode 不依赖模型自我评价，而是用三类证据驱动决策：

**Correctness（正确性验证）**：验证候选是否复现基线行为。支持四种模式——预定义用例（Cases）、参考实现对比（Oracle）、生成器压力测试（Stress）、混合模式（Hybrid）。比较方式从精确匹配到浮点容差到自定义 checker 脚本都有。

**Contract（行为契约）**：在优化前明确公开 API、输入输出、错误行为、状态转换、排序和边界条件。这是一个硬约束——**禁止通过删除检查或修改测试来获得性能提升**。

**Benchmark（可信基准测试）**：这是设计最用心的部分。基线和候选交替执行（interleaved sampling），减少机器状态漂移。统计指标包括中位数、MAD 鲁棒变异率、Bootstrap 置信区间、配对置换检验 p-value。变差超过 15% 直接标记 invalid——宁可不做判断，也不给一个不可信的数字。

优化速度和正确性冲突时，正确性优先。

### 怎么解决"只做局部修补"——算法级分析

Algocode 的 Agent 运行在一个 11 阶段的状态机上：

```
CREATE → ANALYZE → BASELINE → PLAN → GENERATE_CANDIDATE
  → IMPLEMENT → VERIFY → BENCHMARK → COMPARE → DECIDE → REPORT
```

`ANALYZE` 阶段只允许读文件和搜索代码，不能改。Agent 必须分析问题结构、输入分布、操作代数和复杂度差距，提出至少 3 个算法候选方向。`PLAN` 阶段提交结构化的优化计划，包含算法选择、复杂度前后对比和风险分析。

阶段之间有 Gate 门禁：没有 Baseline 不能 Plan，正确性没通过不能 Benchmark，Benchmark 没完成不能 Decide。每个阶段只暴露允许的工具——`ANALYZE` 阶段只有读工具，`IMPLEMENT` 阶段才有编辑工具。

### 怎么解决"没有回滚"——Git Worktree 隔离

Algocode 不直接改你的源文件。它在 Git Worktree 中创建隔离的候选工作区，所有修改在隔离区中进行。验证通过后，你可以查看 Diff、证据报告，再显式执行 apply。应用后发现问题，一键 rollback。

Apply 前还会做 stale 检查——如果你在优化期间手动改过代码，候选会被标记为 STALE 并拒绝应用，防止覆盖你的更改。

## 一个真实案例：Redis SDS 字符串优化

这不是我编的，是项目 README 里的真实案例。

Redis 的 SDS（Simple Dynamic String）实现中，`sdscatprintf` 函数用于格式化追加字符串。工作负载是通过 `sdscatprintf(s, "line %d: hello world\n", i)` 连续追加 10 万行。

**问题**：原始实现无论格式多简单，都走 libc 的 `vsnprintf`——即使只有 `%d` 和字面量，也要计算缓冲区大小、复制 `va_list`、调用 libc。

**优化方案**：Algocode 为受限格式（只有 `%d`、`%s`、`%%` 和字面量）增加了快速路径，在一次遍历中完成格式校验、长度计算和结果写入。复杂格式仍回退到 `vsnprintf`。

关键点是：**输出字节与 libc 格式化保持字节级一致**。不是换了一个格式化实现，而是在简单格式下绕过 libc 的开销。

**验证过程**：
1. Correctness：参考实现与候选在固定格式、边界输入和错误路径上逐字节比对
2. Contract：`sdscatprintf` 的输出字节、长度和可观察 SDS 字段不变
3. Benchmark：交错运行基线和候选，采集多次样本，计算中位数和置信区间

三层全过才进入接受流程。

## 我的思考

AI 辅助编程这件事，目前行业的主要发力点在"生成代码"——写新功能、补全逻辑、修 bug。但"优化现有代码性能"是一个不同的赛道，它有一个核心区别：**优化是有验证标准的**。

写新功能时，"能跑"就是验收标准。但性能优化不行——你必须同时证明"行为没变"和"确实变快了"。这两个证明都不容易，而且 AI 擅长的恰恰是"改代码"，不是"证明改完之后还对"。

Algocode 的思路是把"证明"这件事从模型能力中剥离出来，交给确定性的验证系统。模型负责分析和生成候选，验证系统负责挡住有问题的改动。这种分工比"让模型自己判断改得对不对"靠谱得多。

当然，这个项目也有局限：

- **适用范围窄**：只适合行为可验证的算法代码（竞赛代码、数据结构、图算法等），不适合 Web 开发和业务逻辑
- **不保证正收益**：优化结果可能提升、没变化甚至下降，取决于代码质量、模型能力和运行环境
- **使用有门槛**：需要配置 LLM API、理解契约和验证的概念

但它的价值在于：**即使优化没成功，你也不会得到一个改错的代码**。正确性验证会挡住有问题的候选。

如果你做算法竞赛、写数据结构库、或者维护对性能敏感的组件，可以试试。

**项目地址**：[https://github.com/acfoundry/Algocode](https://github.com/acfoundry/Algocode)

**安装**：

```bash
pip install algocode-agent
```

MIT 开源，支持 OpenAI / DeepSeek / Claude / Kimi / 智谱 GLM / 通义千问等 10+ Provider。

---

以上是我在 AI 辅助代码优化方向的一些观察和实践。如果你也在这个方向探索，欢迎在评论区交流。
